/*
1、万能头文件：#include <bits/stdc++.h>
2、namespace std的作用：省去std::前缀
namespace（命名空间）用来 组织代码和防止命名冲突。
在大型项目中，经常会有不同模块里使用相同的名字
（比如 count、print 等）
如果不加区分，就会报错。
namespace 可以把名字包裹起来，形成“独立空间”，互不干扰。
3、scanf和printf:标准输入输出函数
    scanf("格式字符串", &变量1, &变量2, ...);
    printf("格式字符串", 变量1, 变量2, ...);
    常用格式说明符：
    %d：十进制整数
    %c：单个字符
    %s：字符串
    %f：浮点数（单精度）
    %lf：浮点数（双精度）

4、
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    这三行代码用于优化 C++ 中的输入输出性能。

*/